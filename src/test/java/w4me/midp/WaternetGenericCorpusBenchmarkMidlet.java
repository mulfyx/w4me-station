package w4me.midp;

/** Provides the waternet generic corpus benchmark midlet implementation. */
public final class WaternetGenericCorpusBenchmarkMidlet extends GenericCorpusBenchmarkMidlet {
    /** Performs the workload operation. */
    protected int workload() {
        return WATERNET;
    }
}
