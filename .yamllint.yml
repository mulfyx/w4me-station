---
extends: default

ignore: |
  **/build/**

rules:
  document-start:
    present: true
  line-length:
    max: 120
    allow-non-breakable-inline-mappings: true
  truthy:
    allowed-values:
      - "true"
      - "false"
    check-keys: false
