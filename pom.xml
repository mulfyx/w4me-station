<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.mulfyx</groupId>
    <artifactId>w4me-station-quality</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>W4ME Station quality analysis</name>
    <description>
        Analysis-only build. Release artifacts are built exclusively by tools/build.sh
        with JDK 8, Java 1.3, and the CLDC/MIDP boot class path.
    </description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>8</maven.compiler.source>
        <maven.compiler.target>8</maven.compiler.target>

        <checkstyle.version>13.9.0</checkstyle.version>
        <error-prone.version>2.50.0</error-prone.version>
        <maven-checkstyle-plugin.version>3.6.0</maven-checkstyle-plugin.version>
        <maven-compiler-plugin.version>3.15.0</maven-compiler-plugin.version>
        <maven-pmd-plugin.version>3.28.0</maven-pmd-plugin.version>
        <pmd.version>7.26.0</pmd.version>
        <spotbugs-maven-plugin.version>4.10.3.0</spotbugs-maven-plugin.version>
        <spotless-maven-plugin.version>3.9.0</spotless-maven-plugin.version>
        <palantir-java-format.version>2.96.0</palantir-java-format.version>
    </properties>

    <build>
        <directory>${project.basedir}/build/quality/maven</directory>
        <outputDirectory>${project.basedir}/build/quality/classes</outputDirectory>
        <plugins>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${spotless-maven-plugin.version}</version>
                <configuration>
                    <java>
                        <includes>
                            <include>src/main/java/**/*.java</include>
                            <include>src/test/java/**/*.java</include>
                            <include>bench/configs/**/*.java</include>
                        </includes>
                        <palantirJavaFormat>
                            <version>${palantir-java-format.version}</version>
                            <style>PALANTIR</style>
                            <formatJavadoc>true</formatJavadoc>
                        </palantirJavaFormat>
                        <removeUnusedImports />
                        <trimTrailingWhitespace />
                        <endWithNewline />
                    </java>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>${maven-checkstyle-plugin.version}</version>
                <dependencies>
                    <dependency>
                        <groupId>com.puppycrawl.tools</groupId>
                        <artifactId>checkstyle</artifactId>
                        <version>${checkstyle.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <configLocation>google_checks.xml</configLocation>
                    <consoleOutput>true</consoleOutput>
                    <failOnViolation>true</failOnViolation>
                    <failsOnError>true</failsOnError>
                    <includeTestSourceDirectory>true</includeTestSourceDirectory>
                    <propertyExpansion>
                        org.checkstyle.google.severity=error
                        org.checkstyle.google.suppressionfilter.config=${project.basedir}/config/checkstyle/suppressions.xml
                        org.checkstyle.google.suppressionxpathfilter.config=${project.basedir}/config/checkstyle/suppressions-xpath.xml
                    </propertyExpansion>
                    <violationSeverity>warning</violationSeverity>
                </configuration>
                <executions>
                    <execution>
                        <id>checkstyle</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
                <version>${maven-pmd-plugin.version}</version>
                <dependencies>
                    <dependency>
                        <groupId>net.sourceforge.pmd</groupId>
                        <artifactId>pmd-core</artifactId>
                        <version>${pmd.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>net.sourceforge.pmd</groupId>
                        <artifactId>pmd-java</artifactId>
                        <version>${pmd.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <failOnViolation>true</failOnViolation>
                    <includeTests>true</includeTests>
                    <linkXRef>false</linkXRef>
                    <printFailingErrors>true</printFailingErrors>
                    <rulesets>
                        <ruleset>${project.basedir}/config/pmd/ruleset.xml</ruleset>
                    </rulesets>
                    <targetJdk>1.8</targetJdk>
                </configuration>
                <executions>
                    <execution>
                        <id>pmd</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <compilerArgs>
                        <arg>-XDcompilePolicy=simple</arg>
                        <arg>--should-stop=ifError=FLOW</arg>
                        <arg>-XDaddTypeAnnotationsToSymbol=true</arg>
                        <!--
                            StringBuilder, @Override-on-interface-methods,
                            Integer.valueOf(int), and reflective Constructor
                            APIs require Java 5. MIDP listeners intentionally
                            compare the exact Command/Item instance delivered
                            by LCDUI. The interpreter uses explicit x != x NaN
                            tests to avoid a Java call, exact WebAssembly
                            conversion-boundary literals whose rounded binary
                            values are intentional, and deliberately wrapping
                            32-bit multiplication for the i32.mul opcode.
                        -->
                        <arg>-Xplugin:ErrorProne -Xep:BoxedPrimitiveConstructor:OFF -Xep:ClassNewInstance:OFF -Xep:FloatingPointLiteralPrecision:OFF -Xep:IdentityBinaryExpression:OFF -Xep:JdkObsolete:OFF -Xep:MissingOverride:OFF -Xep:NarrowCalculation:OFF -Xep:ReferenceEquality:OFF</arg>
                        <arg>-Werror</arg>
                        <!--
                            These warnings recommend Java 5+ language/library
                            changes that the Java 1.3 / CLDC production target
                            cannot use. All other javac warnings remain errors.
                        -->
                        <arg>-Xlint:all,-options,-deprecation,-rawtypes,-removal,-strictfp,-unchecked</arg>
                        <!--
                            Error Prone uses javac internals. These exports apply
                            only to the forked JDK 21 analysis compiler, never to
                            the JDK 8 Java ME release compiler.
                        -->
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.api=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.file=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.main=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.model=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.parser=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.processing=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED</arg>
                        <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED</arg>
                        <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.comp=ALL-UNNAMED</arg>
                    </compilerArgs>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>com.google.errorprone</groupId>
                            <artifactId>error_prone_core</artifactId>
                            <version>${error-prone.version}</version>
                        </path>
                    </annotationProcessorPaths>
                    <encoding>UTF-8</encoding>
                    <fork>true</fork>
                    <showWarnings>true</showWarnings>
                </configuration>
            </plugin>

            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <version>${spotbugs-maven-plugin.version}</version>
                <configuration>
                    <effort>Max</effort>
                    <excludeFilterFile>${project.basedir}/config/spotbugs/exclude.xml</excludeFilterFile>
                    <failOnError>true</failOnError>
                    <includeTests>false</includeTests>
                    <maxHeap>1024</maxHeap>
                    <nested>false</nested>
                    <outputDirectory>${project.basedir}/build/quality/spotbugs</outputDirectory>
                    <threshold>Low</threshold>
                    <xmlOutput>true</xmlOutput>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>analysis</id>
            <dependencies>
                <dependency>
                    <groupId>w4me.quality</groupId>
                    <artifactId>cldc-api</artifactId>
                    <version>1</version>
                    <scope>system</scope>
                    <systemPath>${env.CLDC_API_JAR}</systemPath>
                </dependency>
                <dependency>
                    <groupId>w4me.quality</groupId>
                    <artifactId>midp-api</artifactId>
                    <version>1</version>
                    <scope>system</scope>
                    <systemPath>${env.MIDP_API_STUB_JAR}</systemPath>
                </dependency>
                <dependency>
                    <groupId>w4me.quality</groupId>
                    <artifactId>kemulator-api</artifactId>
                    <version>1</version>
                    <scope>system</scope>
                    <systemPath>${env.MIDP_API_JAR}</systemPath>
                </dependency>
            </dependencies>
        </profile>
    </profiles>
</project>
